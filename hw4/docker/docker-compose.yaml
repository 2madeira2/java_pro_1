services:
  postgres:
    image: postgres:15-alpine
    container_name: hw4
    restart: always
    ports:
      - "5444:5432"
    environment:
      POSTGRES_DB: my_db
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    volumes:
      - ./init-scripts:/docker-entrypoint-initdb.d
